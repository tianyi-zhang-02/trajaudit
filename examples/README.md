# Examples

Examples coming in Phase 1+.

Planned:

- `swe_bench_audit.py` — audit a directory of SWE-bench Verified runs.
- `terminal_bench_audit.py` — audit a Terminal-Bench session log.
- `custom_signature.py` — register a project-specific exploit pattern.
- `compare_submissions.py` — programmatic use of the `compare` rank-diff.
